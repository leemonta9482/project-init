# {{AREA}} — 영역 가이드

> 루트 `../CLAUDE.md`를 먼저 읽으십시오. 이 파일은 {{AREA}} 영역 특수 규칙만 다룹니다.

## 스택

- 언어/런타임: {{STACK}}
- 패키지 매니저: **{{PKG_MANAGER}}** (다른 매니저 사용 금지)
- 테스트: {{TEST_FRAMEWORK}}
- 린트·포맷: {{LINTER}}

## 폴더 규칙

```
{{AREA_PATH}}/
├── {{SUBDIR_1}}/   # {{SUBDIR_1_DESC}}
├── {{SUBDIR_2}}/   # {{SUBDIR_2_DESC}}
└── {{SUBDIR_3}}/   # {{SUBDIR_3_DESC}}
```

## 계층 책임

- {{LAYER_RESPONSIBILITY_1}}
- {{LAYER_RESPONSIBILITY_2}}
- 공통 인프라 계층은 도메인을 import하지 않는다(의존 방향 단방향 유지).

## 커밋 scope

- `feat({{AREA}}):`, `fix({{AREA}}):`, `style({{AREA}}):`

## 테스트

- 새 코드는 테스트 동반(TDD: 실패 → 구현 → 통과).
- 외부 의존(DB 등)은 mock보다 **테스트용 실물**(격리된 테스트 인스턴스)을 쓴다.
- 통합 동작은 로컬 실행 환경에서 확인.

## 계약(Contract) 규칙

- 이 영역이 **계약을 제공**한다면(API 응답·스키마), 그 형태의 소유자는 이 영역이다.
- 이 영역이 **계약을 소비**한다면, 응답을 임의 변환하지 않는다 — 형태를 바꿔야 하면 제공 측을 바꾸고 계약 문서에 반영한다.

## 절대 하지 말 것

- 다른 영역 디렉토리({{OTHER_AREAS}}) 수정 금지 — 필요 시 이슈로 요청.
- 패키지 매니저·언어 버전 설정 임의 변경 금지.
- 계약(응답 스키마) 변경 시 상대 영역과 사전 협의 + 변경 제안에 BREAKING CHANGE 명시.
- 계층 규율 경로({{PROTECTED_LAYERS}})를 기준 대조 없이 수정 금지.
